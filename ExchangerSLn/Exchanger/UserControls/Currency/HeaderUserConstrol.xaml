<UserControl x:Class="Exchanger.UserControls.Currency.HeaderUserConstrol" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="1600">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Border Background="#6e849d"  CornerRadius="25,25,0,0"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>


            <Grid Grid.Column="1" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

             
            </Grid>
            <!--sc:BeautyButton Grid.Column="8" Margin="10 5" ButtonStyle="DarkFlat" IsEnabled="{Binding CanGoHome, UpdateSourceTrigger=PropertyChanged}"  Command="{Binding GoToHomeCommand}" Visibility="{Binding IsAuthorized, Converter={StaticResource BooleanToVisibilityConverter}}" Content="{DynamicResource Home}"/-->
        </Grid>
    </Grid>
</UserControl>
